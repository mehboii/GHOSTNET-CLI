# Rust
/target
**/*.rs.bk
Cargo.lock

# Node bridge (installed into ~/.ghostnet-cli at runtime, but ignore local copies too)
node_modules/

# Binary downloaded by the npm postinstall (created at install time)
scripts/bin/
